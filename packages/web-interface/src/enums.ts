export enum LIBRARY_FILE_TYPE {
  NONE,
  FOLDER,
  IMAGE,
  TEXTURE_ATLAS,
}

export enum LIBRARY_ACTION {
  NONE,
  RENAME,
  ADD_FILE,
  ADD_FOLDER,
  DELETE,
  SUBMIT,
  CANCEL,
}

export enum IMAGE_TYPE {
  QUAD,
  POLYGON,
  MESH,
}
