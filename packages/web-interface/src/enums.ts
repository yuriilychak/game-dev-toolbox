export enum LIBRARY_FILE_TYPE {
  NONE = 0,
  FOLDER = 1,
  IMAGE = 2,
  TEXTURE_ATLAS = 3,
}

export enum LIBRARY_ACTION {
  NONE,
  RENAME,
  ADD_FILE,
  ADD_FOLDER,
  DELETE,
  SUBMIT,
  CANCEL,
}
